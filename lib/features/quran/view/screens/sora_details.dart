import 'package:flutter/material.dart';

class SoraDetails extends StatelessWidget {
  const SoraDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("sora details"),),);
  }
}